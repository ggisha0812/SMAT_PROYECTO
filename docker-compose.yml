version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  mqtt_bridge:
    build: ./iot_device
    depends_on:
      - backend
    environment:
      - API_URL=http://backend:8000/lecturas/
